<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.ThemeDemo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Default theme attributes -->
    </style>

    <!-- Game theme -->
    <style name="Theme.ThemeDemo" parent="Base.Theme.ThemeDemo">
        <item name="android:windowBackground">@color/game_background</item>
        <item name="colorPrimary">@color/game_primary</item>
        <item name="colorPrimaryDark">@color/game_primary_dark</item>
        <item name="colorAccent">@color/game_accent</item>
        <item name="android:textColorPrimary">@color/game_text</item>
        <item name="android:textColor">@color/game_text</item>
    </style>
</resources> 